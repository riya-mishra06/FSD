a massive list of 100+ real-world frontend mini projects / interview tasks — grouped by difficulty level 👇

🟢 Beginner (HTML, CSS, Basic JS — 1–30)

Create a button that changes a paragraph’s text when clicked.

Create a page with two images and a button that swaps them.

Create a form that validates name and email on submit.

Add/remove list items dynamically with buttons.

Build a countdown timer that updates in real time.

Create a tabbed interface with different content sections.

Build a progress bar that fills up when a button is clicked.

Make a simple image slider with next/prev buttons.

Create a “Show Password” toggle for a password input field.

Create a digital clock that updates every second.

Make a button that toggles between light and dark mode.

Create a popup modal that opens and closes with buttons.

Build a tooltip that appears on hover.

Create a dropdown menu using only HTML, CSS, and JS.

Build a basic calculator (add, subtract, multiply, divide).

Create a simple to-do list app with add and delete features.

Make a “Back to Top” button appear on scroll.

Create a typing effect animation for text.

Make an accordion with expandable/collapsible panels.

Build a basic image gallery with thumbnails.

Create a responsive navbar that becomes a hamburger menu.

Implement form validation (required fields, email format, etc.).

Add character counter below a textarea input.

Display current date and time dynamically.

Create a button that randomizes the page background color.

Build a simple weather card (use static data).

Make a loading spinner that appears for 2 seconds before showing content.

Create a like button with an increasing count.

Create a star rating component.

Implement smooth scroll behavior for internal links.